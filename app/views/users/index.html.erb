<h1>Users Listing Page</h1>
<div align="center">
  <%= will_paginate %>
</div>
    <div align="left">
      <% @users.each do |user| %>
        <ul class="listing">
          <div class="row">
            <div class="well col-md-4 col-md-offset-4">
              <li><%= link_to gravatar_for(user),user_path(user) %></li>
              <li class="article-title">
                <%= link_to user.username,user_path(user) %>
              </li>
              <li><%= pluralize(user.articles.count,"article") if user.articles %> </li></small>
              <li><% if logged_in? and current_user.admin? %>
                    <%= link_to 'Delete user', user_path(user),method: :delete,data: {confirm: "Are you sure to delte all user data" }%>
                  <% end %>
              </li>
            </div>
          </div>
        </ul>
      <% end %>
    </div>
<div align="center">
  <%= will_paginate %>
</div>
<%= link_to 'Home',root_path %>
